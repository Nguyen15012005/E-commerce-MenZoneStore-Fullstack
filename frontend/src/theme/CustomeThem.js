import { createTheme } from "@mui/material";

const CustomeTheme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#fea928",
    },
    secondary: {
      main: "#ed8900",
    },
  },
});


export default CustomeTheme;